# Adapter generation

`plugin.json`, direct skills, and `catalog/plugins.json` are the authored sources. Platform metadata is generated by `scripts/generate-adapters.py`.

Generated outputs:

- `.agents/plugins/marketplace.json`
- `.claude-plugin/marketplace.json`
- `.grok-plugin/marketplace.json`
- `io.github.algocean1204.clonamic/{codex,claude,grok,hermes}.json`

Run `python3 scripts/generate-adapters.py` after changing a manifest or catalog entry. CI runs the same command with `--check` and fails on drift.

Agent Plugins 1.0.0 does not define marketplace installation, approval UX, hooks, or always-on routing. Adapters translate discovery metadata only. They never copy policy, select models, install children silently, or claim that a host loaded a hook.

Executor packages are omitted from their own host descriptor. A Grok host uses its native agent instead of calling Grok recursively; the same rule applies to Codex, Claude, and Hermes.

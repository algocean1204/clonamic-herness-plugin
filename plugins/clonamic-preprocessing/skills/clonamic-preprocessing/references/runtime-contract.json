{
  "public_surface": "scripts/preprocessing.py",
  "input_fields": ["text", "missing_fields", "queue_path", "loop_auto"],
  "queue_order": ["priority_ascending", "sequence_ascending"],
  "claim_fields": ["worker_id", "claim_id", "claimed_at", "attempts"],
  "write_contract": ["create_new_lock", "bounded_wait", "atomic_replace", "claim_token_match"],
  "loop_states": ["disabled", "drained", "stopped", "limit_reached"],
  "failure_states": ["invalid_input", "missing_item", "executor_error"],
  "path_policy": "Every persistent queue path is supplied by the caller."
}
